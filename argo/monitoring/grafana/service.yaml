apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: argocd
  labels:
    app: grafana
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: grafana
